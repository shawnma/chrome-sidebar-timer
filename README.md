# Sidebar timer for chrome

It creates a timer in chrome's sidebar so it persists across tab switches and scrolling.

To use, download this repo and load it in the chrome extension as "exploded extension".
